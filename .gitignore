# Local data / model outputs
outputs/
data/
*.mp4
wandb/

# HF / caches
.cache/

# Python
__pycache__/
*.pyc

# Editor
.vscode/
.idea/

# Keep .env tracked (no secrets in it) but ignore local overrides.
.env.local

# Saved rerun recordings (large)
rrd/

# openpi training outputs (checkpoints are GB-scale; norm stats are regenerable).
# Anchored to the repo root: unanchored, `assets/` also swallowed sim/*/assets/,
# taking the printed CAD and the scene code with it.
/checkpoints/
/assets/

# Isaac Sim renders and generated USD (regenerate with ./robot sim-assets / sim-play)
sim/outputs/
sim/sim_agent101/assets/usd/
