"""Inspect Robots evaluations for the SO-ARM101.

    rig.py    this bench as an SOArmConfig, from .env + the calibration file
    tasks.py  the benchmarks, embodiment-agnostic so one Task scores every policy
    run.py    the runner: `./robot eval`
"""
